title: explore_collection
description: beatuifulsoup
template:
  repo: explore_collection-template
  name: explore_collection
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
